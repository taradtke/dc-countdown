const express = require('express');
const router = express.Router();

// TODO: Implement carrierNNIs routes

router.get('/', (req, res) => {
  res.json({ message: 'carrierNNIs routes not yet implemented' });
});

module.exports = router;
