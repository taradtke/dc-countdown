const express = require('express');
const router = express.Router();

// TODO: Implement vlans routes

router.get('/', (req, res) => {
  res.json({ message: 'vlans routes not yet implemented' });
});

module.exports = router;
