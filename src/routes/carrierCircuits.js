const express = require('express');
const router = express.Router();

// TODO: Implement carrierCircuits routes

router.get('/', (req, res) => {
  res.json({ message: 'carrierCircuits routes not yet implemented' });
});

module.exports = router;
