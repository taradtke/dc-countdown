const express = require('express');
const router = express.Router();

// TODO: Implement reports routes

router.get('/', (req, res) => {
  res.json({ message: 'reports routes not yet implemented' });
});

module.exports = router;
